# 黑客入侵终端

固定字符缓冲区渲染的本地静态小游戏。玩家或 AI 在节点图上扫描端口、利用漏洞、横向移动，必须在追踪倒计时满格前攻破核心节点。

- `1/2/3/4` 调整播放速度为 `0.5x/1x/2x/4x`
- `P` 暂停，`R` 随机种子重开，`B` 返回总入口
- `Tab` / 方向键切换目标，`Space` 扫描，`Enter` 入侵
- 种子为 100 位 ASCII，输入不足会以空格补齐
