# 打砖块终端

同“类终端游戏”系列风格的本地静态打砖块小游戏。

- 渲染方式：固定字符缓冲区 + canvas 字符绘制。
- 视觉：砖块、球、挡板、拖尾、涟漪、爆裂碎片均由 Braille / block glyph 拼接。
- 模式：`AI DEMO` 自动演示，`HUMAN` 支持键盘和指针控制。
- 操作：`A/D` 或方向键移动，`Space` 暂停，`R` 重开，`1-4` 调速。

启动：

```bat
start.bat
```
