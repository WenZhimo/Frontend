# 病毒扩散终端

固定字符缓冲区渲染的本地静态 Pandemic Board Lite。节点会被病毒感染并沿边扩散，玩家或 AI 需要派遣响应队、隔离高危节点并推进解药研究。

- `1/2/3/4` 调整播放速度为 `0.5x/1x/2x/4x`
- `P` 暂停，`R` 随机种子重开，`V` 返回总入口
- `Tab` / 方向键切换城市，`Space` 派遣响应队，`Q` 隔离节点
- 种子为 100 位 ASCII，输入不足会以空格补齐
