# 乒乓终端

同“类终端游戏”系列风格的本地静态 Pong / 乒乓小游戏。

- 渲染方式：固定字符缓冲区 + canvas 字符绘制。
- 视觉：双挡板、中心网、球、拖尾、反弹火花、得分冲击波均由 Braille / block glyph 拼接。
- 模式：`AI DEMO` 自动对打，`HUMAN VS AI` 单人对 AI，`LOCAL 2P` 本地双人。
- 操作：左侧 `W/S`，右侧 `↑/↓`，`Space` 暂停，`R` 重开，`1-4` 调速，`P` 返回总入口。

启动：

```bat
start.bat
```
